# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for dcgm metrics.
    type: object
    properties:
      gpu.dcgm.clock.frequency:
        description: "GpuDcgmClockFrequencyMetricConfig provides config for the gpu.dcgm.clock.frequency metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.clock.throttle_duration.time:
        description: "GpuDcgmClockThrottleDurationTimeMetricConfig provides config for the gpu.dcgm.clock.throttle_duration.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.codec.decoder.utilization:
        description: "GpuDcgmCodecDecoderUtilizationMetricConfig provides config for the gpu.dcgm.codec.decoder.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.codec.encoder.utilization:
        description: "GpuDcgmCodecEncoderUtilizationMetricConfig provides config for the gpu.dcgm.codec.encoder.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.ecc_errors:
        description: "GpuDcgmEccErrorsMetricConfig provides config for the gpu.dcgm.ecc_errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.energy_consumption:
        description: "GpuDcgmEnergyConsumptionMetricConfig provides config for the gpu.dcgm.energy_consumption metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.memory.bandwidth_utilization:
        description: "GpuDcgmMemoryBandwidthUtilizationMetricConfig provides config for the gpu.dcgm.memory.bandwidth_utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.memory.bytes_used:
        description: "GpuDcgmMemoryBytesUsedMetricConfig provides config for the gpu.dcgm.memory.bytes_used metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.nvlink.io:
        description: "GpuDcgmNvlinkIoMetricConfig provides config for the gpu.dcgm.nvlink.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.pcie.io:
        description: "GpuDcgmPcieIoMetricConfig provides config for the gpu.dcgm.pcie.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.pipe.utilization:
        description: "GpuDcgmPipeUtilizationMetricConfig provides config for the gpu.dcgm.pipe.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.sm.occupancy:
        description: "GpuDcgmSmOccupancyMetricConfig provides config for the gpu.dcgm.sm.occupancy metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      gpu.dcgm.sm.utilization:
        description: "GpuDcgmSmUtilizationMetricConfig provides config for the gpu.dcgm.sm.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.temperature:
        description: "GpuDcgmTemperatureMetricConfig provides config for the gpu.dcgm.temperature metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.utilization:
        description: "GpuDcgmUtilizationMetricConfig provides config for the gpu.dcgm.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      gpu.dcgm.xid_errors:
        description: "GpuDcgmXidErrorsMetricConfig provides config for the gpu.dcgm.xid_errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for dcgm resource attributes.
    type: object
    properties:
      gpu.model:
        description: ResourceAttributeConfig provides common config for a gpu.model resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      gpu.number:
        description: ResourceAttributeConfig provides common config for a gpu.number resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      gpu.uuid:
        description: ResourceAttributeConfig provides common config for a gpu.uuid resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for dcgm metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
