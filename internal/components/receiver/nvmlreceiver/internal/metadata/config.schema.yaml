# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for nvml metrics.
    type: object
    properties:
      nvml.gpu.memory.bytes_used:
        description: "NvmlGpuMemoryBytesUsedMetricConfig provides config for the nvml.gpu.memory.bytes_used metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nvml.gpu.processes.max_bytes_used:
        description: "NvmlGpuProcessesMaxBytesUsedMetricConfig provides config for the nvml.gpu.processes.max_bytes_used metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nvml.gpu.processes.utilization:
        description: "NvmlGpuProcessesUtilizationMetricConfig provides config for the nvml.gpu.processes.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nvml.gpu.utilization:
        description: "NvmlGpuUtilizationMetricConfig provides config for the nvml.gpu.utilization metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for nvml metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
