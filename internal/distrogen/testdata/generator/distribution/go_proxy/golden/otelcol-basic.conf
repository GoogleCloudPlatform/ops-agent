# Systemd environment file for the otelcol-basic service

# Command-line options for the otelcol-basic service.
# Run `/usr/bin/otelcol-basic --help` to see all available options.
OTELCOL_OPTIONS="--config=/etc/otelcol-basic/config.yaml"
