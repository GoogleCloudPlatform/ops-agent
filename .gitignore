# Ignore a file actually called Makefile. The presence of this file interferes
# with our Linux package building processes. Use the command:
# make -f tasks.mak makefile_symlink
# To make a local symlinked Makefile for development convenience.
Makefile

# Ignore generated credentials from google-github-actions/auth
gha-creds-*.json

# This folder is used to contain local builds of fluent-bit and otelopscol
# for running transformation tests locally.
dist/