metrics:
  receivers:
    hostmetrics:
      type: hostmetrics
      collection_interval: 60s
  processors:
    processor_1:
      type: unsupported_type
  service:
    pipelines:
      default_pipeline:
        receivers: [hostmetrics]
