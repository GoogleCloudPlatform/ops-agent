logging:
  processors:
    processor_1:
      type: exclude_logs
      condition: jsonPayload.message = "foo\[bar\]"
  service:
    pipelines:
      default_pipeline:
        processors: [processor_1]
