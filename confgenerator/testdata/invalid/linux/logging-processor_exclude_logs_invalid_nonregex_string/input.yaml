logging:
  processors:
    processor_1:
      type: exclude_logs
      conditions: ['jsonPayload.message = "foo\[bar\]"']
  service:
    pipelines:
      default_pipeline:
        processors: [processor_1]
