logging:
  receivers:
    receiver_1:
      type: files
      include_paths:
      - /var/log/messages
      - /var/log/syslog
      channels: value_1
  exporters:
    google:
      type: google_cloud_logging
  service:
    pipelines:
      default_pipeline:
        receivers: [receiver_1]
        exporters: [google]
