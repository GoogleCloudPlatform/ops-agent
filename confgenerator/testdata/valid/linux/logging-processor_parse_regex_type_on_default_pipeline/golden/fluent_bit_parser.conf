[PARSER]
    Format      regex
    Name        default_pipeline.syslog.0
    Regex       regex_pattern_1
    Time_Format time_format_1
    Time_Key    time_key_1

[PARSER]
    Format      regex
    Name        logging-severity-parser.self-logs-severity
    Regex       ^\[[ ]*(?<severity>[a-z]+)\]
    Time_Format %d/%b/%Y:%H:%M:%S %z
    Time_Key    time
    Types       severity:string
