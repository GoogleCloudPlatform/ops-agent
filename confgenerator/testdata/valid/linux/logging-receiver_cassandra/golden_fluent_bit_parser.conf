[PARSER]
    Format      regex
    Name        cassandra_custom.cassandra_custom_system.cassandra_system
    Regex       (?<level>[A-Z]+)\s+\[(?<type>[^\]]+)\]\s+(?<time>\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2},\d+)\s+(?<extendedMessage>(?<message>(?:(?<javaClass>[\w\.]+):(?<lineNumber>\d+))?.+)[\S\s]+)
    Time_Format %Y-%m-%d %H:%M:%S,%L
    Time_Key    time
    Types       lineNumber:integer

[PARSER]
    Format      regex
    Name        cassandra_default.cassandra_default_system.cassandra_system
    Regex       (?<level>[A-Z]+)\s+\[(?<type>[^\]]+)\]\s+(?<time>\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2},\d+)\s+(?<extendedMessage>(?<message>(?:(?<javaClass>[\w\.]+):(?<lineNumber>\d+))?.+)[\S\s]+)
    Time_Format %Y-%m-%d %H:%M:%S,%L
    Time_Key    time
    Types       lineNumber:integer

[PARSER]
    Format regex
    Name   cassandra_syslog_system.cassandra_syslog_system
    Regex  ^(?<message>.*)$

[PARSER]
    Format      regex
    Name        cassandra_syslog_system.cassandra_syslog_system.0
    Regex       (?<level>[A-Z]+)\s+\[(?<type>[^\]]+)\]\s+(?<time>\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2},\d+)\s+(?<extendedMessage>(?<message>(?:(?<javaClass>[\w\.]+):(?<lineNumber>\d+))?.+)[\S\s]+)
    Time_Format %Y-%m-%d %H:%M:%S,%L
    Time_Key    time
    Types       lineNumber:integer

[MULTILINE_PARSER]
    Name cassandra_custom.cassandra_custom_system.cassandra_system.multiline
    Type regex
    rule    "start_state"    "[A-Z]+\s+\[[^\]]+\] \d+"    "cont"
    rule    "cont"    "^(?![A-Z]+\s+\[[^\]]+\] \d+)"    "cont"

[MULTILINE_PARSER]
    Name cassandra_default.cassandra_default_system.cassandra_system.multiline
    Type regex
    rule    "start_state"    "[A-Z]+\s+\[[^\]]+\] \d+"    "cont"
    rule    "cont"    "^(?![A-Z]+\s+\[[^\]]+\] \d+)"    "cont"

[MULTILINE_PARSER]
    Name cassandra_syslog_system.cassandra_syslog_system.0.multiline
    Type regex
    rule    "start_state"    "[A-Z]+\s+\[[^\]]+\] \d+"    "cont"
    rule    "cont"    "^(?![A-Z]+\s+\[[^\]]+\] \d+)"    "cont"
