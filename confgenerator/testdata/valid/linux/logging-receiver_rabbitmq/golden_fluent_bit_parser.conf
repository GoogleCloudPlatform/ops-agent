[PARSER]
    Format      regex
    Name        rabbitmq_default.rabbitmq_custom.rabbitmq
    Regex       ^(?<timestamp>\d+-\d+-\d+ \d+:\d+:\d+\.\d+\+\d+:\d+) \[(?<severity>\w+)\] \<(?<process_id>\d+\.\d+\.\d+)\>(?<message>.*)
    Time_Format %Y-%m-%d %H:%M:%S.%L+%Z
    Time_Key    timestamp

[PARSER]
    Format      regex
    Name        rabbitmq_default.rabbitmq_default.rabbitmq
    Regex       ^(?<timestamp>\d+-\d+-\d+ \d+:\d+:\d+\.\d+\+\d+:\d+) \[(?<severity>\w+)\] \<(?<process_id>\d+\.\d+\.\d+)\>(?<message>.*)
    Time_Format %Y-%m-%d %H:%M:%S.%L+%Z
    Time_Key    timestamp

[MULTILINE_PARSER]
    flush_timeout 5000
    name          multiline.rabbitmq_default.rabbitmq_custom
    type          regex
    rule          "start_state"    "^(?<timestamp>\d+-\d+-\d+ \d+:\d+:\d+\.\d+\+\d+:\d+) \[(?<severity>\w+)\] \<(?<process_id>\d+\.\d+\.\d+)\>(?<message>.*)$"    "cont"
    rule          "cont"    "^\s*$"    "cont"

[MULTILINE_PARSER]
    flush_timeout 5000
    name          multiline.rabbitmq_default.rabbitmq_default
    type          regex
    rule          "start_state"    "^(?<timestamp>\d+-\d+-\d+ \d+:\d+:\d+\.\d+\+\d+:\d+) \[(?<severity>\w+)\] \<(?<process_id>\d+\.\d+\.\d+)\>(?<message>.*)$"    "cont"
    rule          "cont"    "^\s*$"    "cont"
