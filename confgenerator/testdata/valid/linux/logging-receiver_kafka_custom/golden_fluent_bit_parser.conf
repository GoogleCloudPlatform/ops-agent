[PARSER]
    Format      regex
    Name        kafka_custom.kafka_custom.kafka
    Regex       ^\[(?<time>\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2},\d+)\]\s+(?<level>[A-Z]+)(?:\s+\[(?<source>.*)\]:?)?\s+(?<message>[\s\S]*?)(?=\s+\([\w\s\.\$]+\)$|\s+$)(?:\s+\((?<logger>[\w\s\.\$]+)\))?
    Time_Format %Y-%m-%d %H:%M:%S,%L
    Time_Key    time

[PARSER]
    Format regex
    Name   kafka_syslog.kafka_syslog
    Regex  ^(?<message>.*)$

[PARSER]
    Format      regex
    Name        kafka_syslog.kafka_syslog.0
    Regex       ^\[(?<time>\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2},\d+)\]\s+(?<level>[A-Z]+)(?:\s+\[(?<source>.*)\]:?)?\s+(?<message>[\s\S]*?)(?=\s+\([\w\s\.\$]+\)$|\s+$)(?:\s+\((?<logger>[\w\s\.\$]+)\))?
    Time_Format %Y-%m-%d %H:%M:%S,%L
    Time_Key    time

[MULTILINE_PARSER]
    flush_timeout 5000
    name          multiline.kafka_custom.kafka_custom
    type          regex
    rule          "start_state"    "\[\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2},\d+\]"    "cont"
    rule          "cont"    "^(?!\[\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2},\d+\])"    "cont"
