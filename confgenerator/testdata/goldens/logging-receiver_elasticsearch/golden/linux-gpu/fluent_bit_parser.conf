[PARSER]
    Format      regex
    Name        elasticsearch.elasticsearch_gc.0
    Regex       \[(?<time>\d+-\d+-\d+T\d+:\d+:\d+.\d+\+\d+)\]\[\d+\]\[(?<type>[A-z,]+)\s*\]\s*(?:GC\((?<gc_run>\d+)\))?\s*(?<message>.*)
    Time_Format %Y-%m-%dT%H:%M:%S.%L%z
    Time_Key    time
    Types       gc_run:integer

[PARSER]
    Format json
    Name   elasticsearch.elasticsearch_json.0

[PARSER]
    Format      regex
    Name        elasticsearch.elasticsearch_json.1
    Regex       (?<temp_time_key>.*)
    Time_Format %Y-%m-%dT%H:%M:%S,%L%z
    Time_Key    temp_time_key

[PARSER]
    Format      regex
    Name        elasticsearch.elasticsearch_json.2
    Regex       (?<temp_time_key>.*)
    Time_Format %Y-%m-%dT%H:%M:%S.%L%z
    Time_Key    temp_time_key

[PARSER]
    Format regex
    Name   elasticsearch.elasticsearch_json.3
    Regex  (?<temp_level>.*)

[PARSER]
    Format regex
    Name   elasticsearch.elasticsearch_json.4
    Regex  (?<temp_level>.*)

[PARSER]
    Format      regex
    Name        ops-agent-fluent-bit.fluent-bit-self-log-regex-parsing
    Regex       (?<message>\[[ ]*(?<time>\d+\/\d+\/\d+ \d+:\d+:\d+)] \[[ ]*(?<severity>[a-z]+)\].*)
    Time_Format %Y/%m/%d %H:%M:%S
    Time_Key    time
    Types       severity:string

[PARSER]
    Format      json
    Name        ops-agent-health.health-checks-json
    Time_Format %Y-%m-%dT%H:%M:%S%z
    Time_Key    time

[MULTILINE_PARSER]
    flush_timeout 1000
    name          multiline.elasticsearch.elasticsearch_json
    type          regex
    rule          "start_state"    "^{.*"    "cont"
    rule          "cont"    "^[^{].*[,}]$"    "cont"
