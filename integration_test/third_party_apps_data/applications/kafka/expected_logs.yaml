log_entries:
- log_name: "kafka_default"
  field_matchers:
    jsonPayload.message: ".*Waiting until connected.*"
    jsonPayload.source: "ZooKeeperClient Kafka server"
    jsonPayload.logger: "kafka.zookeeper.ZooKeeperClient"
