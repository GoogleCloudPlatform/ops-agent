# SAP HANA is pre-installed on the images used for tests

sudo su - opsadm -c '/usr/sap/OPS/HDB00/exe/hdbsql -u SYSTEM -p G00g3l21 -j "SELECT * from M_LICENSE"'
sudo su - opsadm -c '/usr/sap/OPS/HDB00/exe/hdbsql -u SYSTEM -j "SELECT * from M_LICENSE"'

hdbsql -u SYSTEM -p G00g3l21 -j "SELECT * from M_LICENSE"
hdbsql -u SYSTEM -j "SELECT * from M_LICENSE"

set -e

# Check that the instance is up
sudo su - opsadm -c '/usr/sap/OPS/HDB00/exe/hdbsql -u SYSTEM -p G00g3l21 -j "SELECT * FROM DUMMY"'
