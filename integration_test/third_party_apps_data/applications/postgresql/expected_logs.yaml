log_entries:
- log_name: "postgresql_general"
  field_matchers:
    jsonPayload.message: '.*Connection matched pg_hba.conf.*'
    jsonPayload.level: "DETAIL"
    jsonPayload.user: "postgres"
    jsonPayload.role: "postgres"
