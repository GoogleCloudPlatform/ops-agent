set -e

# Create a file that nginx can't read.
if [ -d "/usr/share/nginx/html/" ] 
then
    # This directory is used by RHEL9
    sudo touch /usr/share/nginx/html/forbidden.html
    sudo chmod o-r /usr/share/nginx/html/forbidden.html
else
    sudo touch /var/www/html/forbidden.html
    sudo chmod o-r /var/www/html/forbidden.html
fi

# Then request that file through nginx. This is meant to generate an entry in
# the access log and the error log.
curl http://localhost:80/forbidden.html
