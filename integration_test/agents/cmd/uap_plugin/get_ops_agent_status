# A curl command that sends a GetStatus gRPC request to the Ops Agent UAP Plugin gRPC server.

set -e

printf '\x00\x00\x00\x00\x00' | curl --http2-prior-knowledge  -H 'Content-Type: application/grpc' --output get_status_reponse --data-binary @- localhost:1234/plugin_comm.GuestAgentPlugin/GetStatus

cat get_status_reponse
